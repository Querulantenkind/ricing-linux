# Hyprland Color Snippet - Nord
# Location: ~/.config/hyprland/hyprland.conf
# Usage: Copy these sections into your hyprland.conf file

# ===== General Settings =====
general {
    gaps_in = 10
    gaps_out = 10
    border_size = 2
    
    # Border colors
    col.active_border = rgb(88c0d0)      # Accent cyan
    col.inactive_border = rgb(434c5e)    # Surface0
    
    layout = dwindle
}

# ===== Decoration =====
decoration {
    rounding = 8
    
    blur {
        enabled = true
        size = 3
        passes = 1
        new_optimizations = true
    }
    
    drop_shadow = yes
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(2e3440ee)          # BG with alpha
    
    # Dim inactive windows
    dim_inactive = true
    dim_strength = 0.1
}

# ===== Animations =====
animations {
    enabled = yes
    
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    
    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

# ===== Miscellaneous =====
misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
    background_color = rgb(2e3440)       # BG
}

# ===== Group Colors (Window Grouping) =====
group {
    col.border_active = rgb(88c0d0)      # Accent
    col.border_inactive = rgb(434c5e)    # Surface0
    
    groupbar {
        font_family = JetBrains Mono
        font_size = 11
        col.active = rgb(88c0d0)         # Accent
        col.inactive = rgb(434c5e)       # Surface0
    }
}
